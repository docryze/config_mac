# Path to your Oh My Zsh installation.
export ZSH="$HOME/.oh-my-zsh"

# Set name of the theme to load --- if set to "random", it will
ZSH_THEME="robbyrussell"

# Which plugins would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(
    zsh-autosuggestions
    zsh-syntax-highlighting
    zsh-history-substring-search
#    git
#    git-commit

    vi-mode
)

export EDITOR=vim

fpath+=${ZSH_CUSTOM:-${ZSH:-~/.oh-my-zsh}/custom}/plugins/zsh-completions/src
autoload -U compinit && compinit
source "$ZSH/oh-my-zsh.sh"

# homebrew 
export HOMEBREW_NO_AUTO_UPDATE=1

# script
export SCRIPT_HOME="$HOME/.script"
export PATH=$SCRIPT_HOME:$PATH

# key
source ~/.access_key_rc

# nvm 
#export NVM_DIR="$([ -z "${XDG_CONFIG_HOME-}" ] && printf %s "${HOME}/.nvm" || printf %s "${XDG_CONFIG_HOME}/nvm")"
##[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" --no-use # This loads nvm, without auto-using the default version
#[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm, without auto-using the default version
#[ -s "/usr/local/opt/nvm/etc/bash_completion.d/nvm" ] && \. "/usr/local/opt/nvm/etc/bash_completion.d/nvm"  # This loads nvm bash_completion
#alias node='nvm use default && node'
# fnm
eval "$(fnm env --use-on-cd --shell zsh)"

# bun
export BUN_HOME="$HOME/.bun"
export PATH=$BUN_HOME/bin:$PATH

# pyenv
#export PYENV_ROOT="$HOME/.pyenv"
#[[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"
#eval "$(pyenv init - zsh)"

# jdk
#export JAVA_HOME="$HOME/soft/jdk17/Contents/Home"
export JAVA_HOME="$HOME/soft/jdk21/Contents/Home"
export PATH=$JAVA_HOME/bin:$PATH

# go
export GOROOT="$HOME/soft/go.1.24.4"
export PATH=$GOROOT/bin:$PATH
export GOPATH="$HOME/go"
export PATH=$GOPATH/bin:$PATH

# flutter
export FLUTTER_PATH="$HOME/soft/flutter"
export PATH=$FLUTTER_PATH/bin:$PATH

# rust
#export RUST_PATH="$HOME/.cargo"
#export PATH=$RUST_PATH/bin:$PATH

# oss util
export OSS_UTIL_PATH="$HOME/soft/ossutil-2.2.0-mac-amd64"
export PATH=$OSS_UTIL_PATH:$PATH


# jmeter
export JMETER_HOME="$HOME/soft/apache-jmeter-5.6.3"
export PATH=$JMETER_HOME/bin:$PATH

# mvn
export MAVEN_HOME="$HOME/soft/apache-maven-3.9.9"
export PATH=$MAVEN_HOME/bin:$PATH

#vcpkg
export VCPKG_ROOT="$HOME/soft/vcpkg"
export PATH=$VCPKG_ROOT:$PATH


# pnpm
export PNPM_HOME="/Users/docryze/Library/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end


# customer command
gco(){
    git branch | fzf | xargs git checkout
}
gsp(){
    git stash list | fzf | awk -F '{|}' '{print $2}' | xargs git stash pop
}
export PATH="/usr/local/opt/mysql-client/bin:$PATH"
export PATH="/usr/local/sbin:$PATH"

# bun completions
[ -s "/Users/docryze/.bun/_bun" ] && source "/Users/docryze/.bun/_bun"

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

# opencode
export OPENCODE_DISABLE_LSP_DOWNLOAD=true
#compdef opencode
###-begin-opencode-completions-###
#
# yargs command completion script
#
# Installation: opencode completion >> ~/.zshrc
#    or opencode completion >> ~/.zprofile on OSX.
#
_opencode_yargs_completions()
{
  local reply
  local si=$IFS
  IFS=$'
' reply=($(COMP_CWORD="$((CURRENT-1))" COMP_LINE="$BUFFER" COMP_POINT="$CURSOR" opencode --get-yargs-completions "${words[@]}"))
  IFS=$si
  if [[ ${#reply} -gt 0 ]]; then
    _describe 'values' reply
  else
    _default
  fi
}
if [[ "'${zsh_eval_context[-1]}" == "loadautofunc" ]]; then
  _opencode_yargs_completions "$@"
else
  compdef _opencode_yargs_completions opencode
fi
###-end-opencode-completions-###

